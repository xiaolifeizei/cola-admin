# 数据源配置
spring:
  redis:
    host: localhost
    port: 6379

# Dubbo配置
dubbo:
  application:
    register-mode: instance
  protocol:
    name: dubbo
  provider:
    retries: 0
  consumer:
    check: false
    timeout: 5000
    retries: 0
